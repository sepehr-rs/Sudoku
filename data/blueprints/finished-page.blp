using Gtk 4.0;
using Adw 1;

template $FinishedPage: Box {
  hexpand: true;
  vexpand: true;
  margin-bottom: 30;

  Box {
    orientation: vertical;
    spacing: 12;
    margin-bottom: 20;
    halign: fill;
    valign: center;
    hexpand: true;
    vexpand: true;

    Adw.Clamp {
      maximum-size: 1200;
      tightening-threshold: 300;

      Box {
        orientation: vertical;
        spacing: 12;

        Picture picture_contain {
          halign: center;
          valign: fill;
          hexpand: true;
          vexpand: true;
          margin-bottom: 60;
          can-shrink: true;
          content-fit: contain;
        }

        Label finished_label {
          wrap: true;
          justify: center;
          styles [
            "title-1",
          ]
        }
      }
    }
  }
}
