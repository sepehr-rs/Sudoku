modulesubdir = join_paths(moduledir, 'core')

services_sources = [
    'board.py',
    'constants.py',
    'controller.py',
    'generator.py',
    'persistence.py',
    'preferences.py'
]

install_data(services_sources, install_dir: modulesubdir)
